Generic microchip vendor RAM wrapper placeholders.

These modules are explicit migration points. The default implementation is
portable inference (`af_ram_tdp`) and can be replaced per-board in production.
