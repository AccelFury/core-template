Generic xilinx vendor RAM wrapper placeholders.

These modules are explicit migration points. The default implementation is
portable inference (`af_ram_tdp`) and remains deterministic for simulation.
